# ============================================================================
# Crux-Webmail Enterprise — Docker Compose Zero-Trust Orchestration
# ============================================================================
# Principios:
#   - Microsegmentación via Docker networks isolated
#   - mTLS entre servicios internos
#   - read_only filesystems + tmpfs para escritura temporal
#   - noNewPrivileges, runAsNonRoot
#   - Health checks estrictos con restart policy
#   - Default DROPPING de tráfico inter-red
# ============================================================================

# ------------------------------------------------------------------
# NETWORKS: Segmentación Zero-Trust
# ------------------------------------------------------------------
networks:
  public-network:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.20.0.0/24
    # Solo nginx expone puertos al host
  app-network:
    driver: bridge
    internal: true
    ipam:
      driver: default
      config:
        - subnet: 172.21.0.0/24
  data-network:
    driver: bridge
    internal: true
    ipam:
      driver: default
      config:
        - subnet: 172.22.0.0/24
  monitor-network:
    driver: bridge
    internal: true
    ipam:
      driver: default
      config:
        - subnet: 172.23.0.0/24

# ------------------------------------------------------------------
# VOLUMES: Persistencia inmutable
# ------------------------------------------------------------------
volumes:
  postfix_mail:
    driver: local
  postfix_queue:
    driver: local
  dovecot_mail:
    driver: local
  dovecot_db:
    driver: local
  amavis_db:
    driver: local
  clamav_db:
    driver: local
  postgres_data:
    driver: local
  redis_data:
    driver: local
  minio_data:
    driver: local
  logs_shared:
    driver: local
  grafana_data:
    driver: local
  prometheus_data:
    driver: local
  alertmanager_data:
    driver: local

# ------------------------------------------------------------------
# SERVICES
# ------------------------------------------------------------------
services:
  # =================================================================
  # DMZ LAYER — Edge Proxy
  # =================================================================
  nginx:
    image: nginx:1.27-alpine
    container_name: crux-nginx
    networks:
      public-network:
        ipv4_address: 172.20.0.10
      app-network:
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./infra/nginx/certs:/etc/nginx/certs:ro
      - logs_shared:/var/log/nginx
    tmpfs:
      - /var/cache/nginx:size=128M
      - /var/run:size=64M
      - /tmp:size=64M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:80/ > /dev/null 2>&1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE  # Solo para puertos < 1024
    environment:
      - NGINX_WORKER_PROCESSES=auto
      - TZ=UTC
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"
    depends_on:
      postfix:
        condition: service_healthy
      fastify-backend:
        condition: service_healthy
      nextjs-frontend:
        condition: service_healthy

  # =================================================================
  # APP-TIER — MTA: Postfix
  # =================================================================
  postfix:
    image: mwader/postfix:latest
    container_name: crux-postfix
    networks:
      app-network:
        ipv4_address: 172.21.0.10
      data-network:
    expose:
      - "25"
      - "587"
    volumes:
      - ./infra/postfix/main.cf:/etc/postfix/main.cf:ro
      - ./infra/postfix/master.cf:/etc/postfix/master.cf:ro
      - postfix_mail:/var/mail:rw
      - postfix_queue:/var/spool/postfix:rw
      - logs_shared:/var/log/postfix
      - ./infra/certs:/etc/crux-certs:ro
    tmpfs:
      - /var/spool/postfix/dev:size=64M
      - /var/spool/postfix/pid:size=64M
      - /tmp:size=64M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "postfix status && ss -tlnp | grep -q ':25 '"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - SETGID
      - SETUID
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SYS_CHROOT
    environment:
      - TZ=UTC
      - TZDATA_DIR=/usr/share/zoneinfo
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # APP-TIER — LDA/IMAP: Dovecot
  # =================================================================
  dovecot:
    image: mwader/dovecot:latest
    container_name: crux-dovecot
    networks:
      app-network:
        ipv4_address: 172.21.0.11
      data-network:
    expose:
      - "993"
      - "995"
      - "143"
    volumes:
      - ./infra/dovecot/dovecot.conf:/etc/dovecot/dovecot.conf:ro
      - ./infra/dovecot/conf.d/10-ssl.conf:/etc/dovecot/conf.d/10-ssl.conf:ro
      - ./infra/dovecot/conf.d/20-lmtp.conf:/etc/dovecot/conf.d/20-lmtp.conf:ro
      - ./infra/dovecot/conf.d/10-mail.conf:/etc/dovecot/conf.d/10-mail.conf:ro
      - ./infra/dovecot/conf.d/10-auth.conf:/etc/dovecot/conf.d/10-auth.conf:ro
      - dovecot_mail:/var/mail:rw
      - dovecot_db:/var/lib/dovecot:rw
      - logs_shared:/var/log/dovecot
      - ./infra/certs:/etc/crux-certs:ro
    tmpfs:
      - /var/run/dovecot:size=64M
      - /tmp:size=64M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "dovecot status && ss -tlnp | grep -q ':993 '"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - SETGID
    environment:
      - DOVECOT_MASTER_PASSWORD_FILE=/run/secrets/dovecot_master_password
      - TZ=UTC
    secrets:
      - dovecot_master_password
    user: "1000:1000"
    depends_on:
      postgres:
        condition: service_healthy
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # APP-TIER — Content Filter: Amavis
  # ==========================================================================
  amavis:
    image: mwader/amavis:latest
    container_name: crux-amavis
    networks:
      app-network:
        ipv4_address: 172.21.0.12
      data-network:
    expose:
      - "10024"
      - "9998"
    volumes:
      - ./infra/amavis/amavis.conf:/etc/amavis/amavis.conf:ro
      - amavis_db:/var/lib/amavis:rw
      - logs_shared:/var/log/amavis
    tmpfs:
      - /tmp:size=256M
      - /var/amavis/tmp:size=256M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "amavisd-is-running || ss -tlnp | grep -q ':10024 '"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 45s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
    environment:
      - TZ=UTC
    user: "1000:1000"
    depends_on:
      clamav:
        condition: service_healthy
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # DATA-TIER — Antivirus Worker: ClamAV
  # =================================================================
  clamav:
    image: clamav/clamav:latest
    container_name: crux-clamav
    networks:
      data-network:
        ipv4_address: 172.22.0.10
    expose:
      - "3310"
    volumes:
      - clamav_db:/var/lib/clamav:rw
      - logs_shared:/var/log/clamav
    tmpfs:
      - /tmp:size=256M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "clamd --version || ss -tlnp | grep -q ':3310 '"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 60s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    environment:
      - TZ=UTC
    deploy:
      resources:
        limits:
          memory: 2G
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # DATA-TIER — Database: PostgreSQL
  # =================================================================
  postgres:
    image: postgres:17-alpine
    container_name: crux-postgres
    networks:
      data-network:
        ipv4_address: 172.22.0.11
      monitor-network:
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data:rw
    tmpfs:
      - /tmp:size=64M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crux_user -d crux_mail || exit 1"]
      interval: 5s
      timeout: 2s
      retries: 5
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
      - CHOWN
      - NET_BIND_SERVICE
    environment:
      - POSTGRES_DB=crux_mail
      - POSTGRES_USER=crux_user
      - POSTGRES_PASSWORD_FILE=/run/secrets/postgres_password
      - POSTGRES_INITDB_ARGS=--data-checksums
      - TZ=UTC
    secrets:
      - postgres_password
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # DATA-TIER — Cache/Rate-limiting: Redis
  # =================================================================
  redis:
    image: redis:7-alpine
    container_name: crux-redis
    networks:
      app-network:
        ipv4_address: 172.21.0.15
      data-network:
      monitor-network:
    expose:
      - "6379"
    volumes:
      - redis_data:/data:rw
      - ./redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
    tmpfs:
      - /tmp:size=32M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep -q PONG"]
      interval: 5s
      timeout: 2s
      retries: 5
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - DAC_OVERRIDE
    command: >-
      sh -c 'exec redis-server /usr/local/etc/redis/redis.conf --requirepass "$(cat /run/secrets/redis_password)"'
    environment:
      - TZ=UTC
    secrets:
      - redis_password
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # DATA-TIER — Object Storage: MinIO (Attachments)
  # =================================================================
  minio:
    image: minio/minio:latest
    container_name: crux-minio
    networks:
      data-network:
        ipv4_address: 172.22.0.12
      monitor-network:
    expose:
      - "9000"
      - "9001"
    volumes:
      - minio_data:/data:rw
    command: >-
      sh -c 'export MINIO_ROOT_USER="$(cat /run/secrets/minio_access_key)" &&
             export MINIO_ROOT_PASSWORD="$(cat /run/secrets/minio_secret_key)" &&
             exec minio server /data --console-address ":9001"'
    tmpfs:
      - /tmp:size=32M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget --spider http://localhost:9000/minio/health/live || exit 1"]
      interval: 5s
      timeout: 2s
      retries: 5
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
    environment:
      - MINIO_SERVER_URL=https://storage.crux.local
      - TZ=UTC
    secrets:
      - minio_access_key
      - minio_secret_key
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # MONITORING — Grafana (dashboards + provisioning)
  # =================================================================
  grafana:
    image: grafana/grafana:11.2.0-alpine
    container_name: crux-grafana
    networks:
      monitor-network:
        ipv4_address: 172.23.0.10
      public-network:
        ipv4_address: 172.20.0.30
    expose:
      - "3000"  # Accessed via nginx reverse proxy only
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/provisioning/dashboards/files:/etc/grafana/provisioning/dashboards/files:ro
      - grafana_data:/var/lib/grafana:rw
    tmpfs:
      - /tmp:size=32M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health | grep -q 'OK'"]
      interval: 15s
      timeout: 3s
      retries: 3
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD_FILE=/run/secrets/grafana_admin_password
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_ANALYTICS_REPORTING_ENABLED=false
      - TZ=UTC
    secrets:
      - grafana_admin_password
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # MONITORING — Prometheus (metrics scraping + alerting)
  # =================================================================
  prometheus:
    image: prom/prometheus:v2.53.0
    container_name: crux-prometheus
    networks:
      monitor-network:
        ipv4_address: 172.23.0.20
    expose:
      - "9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/prometheus/rules:/etc/prometheus/rules:ro
      - prometheus_data:/prometheus:rw
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--storage.tsdb.retention.size=10GB'
      - '--web.enable-lifecycle'
      - '--web.enable-admin-api=false'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
    tmpfs:
      - /tmp:size=32M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9090/-/healthy | grep -q 'Prometheus Server is Healthy'"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 2G
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # MONITORING — Alertmanager (alert routing)
  # =================================================================
  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: crux-alertmanager
    networks:
      monitor-network:
        ipv4_address: 172.23.0.21
    expose:
      - "9093"
    volumes:
      - ./monitoring/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager_data:/alertmanager:rw
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--web.enable-lifecycle'
    environment:
      - SLACK_WEBHOOK_URL_FILE=/run/secrets/alertmanager_webhook_url
    secrets:
      - alertmanager_webhook_url
    tmpfs:
      - /tmp:size=32M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9093/-/healthy | grep -q 'OK'"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # MONITORING — Redis Exporter (Redis metrics for Prometheus)
  # =================================================================
  redis-exporter:
    image: oliver006/redis_exporter:v1.58.0
    container_name: crux-redis-exporter
    networks:
      monitor-network:
        ipv4_address: 172.23.0.30
      data-network:
    expose:
      - "9121"
    environment:
      - REDIS_PASSWORD_FILE=/run/secrets/redis_password
      - REDIS_ADDR=redis://crux-redis:6379
      - REDIS_EXPORTER_LOG_LEVEL=warning
      - REDIS_EXPORTER_CHECK_DUPLICATE_QUERIES=true
    secrets:
      - redis_password
    tmpfs:
      - /tmp:size=32M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9121/metrics | grep -q 'redis_up'"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
        tag: "{{.Name}}"
    depends_on:
      redis:
        condition: service_healthy

  # =================================================================
  # MONITORING — PostgreSQL Exporter (PostgreSQL metrics)
  # ============================================================================
  postgres-exporter:
    image: promcommunity/postgres_exporter:v0.15.0
    container_name: crux-postgres-exporter
    networks:
      monitor-network:
        ipv4_address: 172.23.0.31
      data-network:
    expose:
      - "9187"
    environment:
      - DATA_SOURCE_NAME=postgresql://crux_user@crux-postgres:5432/crux_mail?sslmode=disable
      - PG_EXPORTER_EXTEND_QUERY_PATH=
    secrets:
      - postgres_password
    command: >
      sh -c "export PGPASSWORD=$(cat /run/secrets/postgres_password) && exec /postgres_exporter"
    tmpfs:
      - /tmp:size=32M
    read_only: true
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9187/metrics | grep -q 'pg_up'"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
        tag: "{{.Name}}"
    depends_on:
      postgres:
        condition: service_healthy

  # =================================================================
  # MONITORING — Node Exporter (system-level: CPU, memory, disk, net)
  # =================================================================
  node-exporter:
    image: prom/node-exporter:v1.8.2
    container_name: crux-node-exporter
    networks:
      monitor-network:
        ipv4_address: 172.23.0.32
    expose:
      - "9100"
    command:
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($/)'
      - '--collector.netclass.device-exclude=lo'
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_RAW
    pid: host
    user: "65534:65534"  # nobody:nobody
    read_only: true
    tmpfs:
      - /tmp:size=32M
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9100/metrics | grep -q 'node_exporter_build_info'"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
        tag: "{{.Name}}"

  # =================================================================
  # MONITORING — Loki (Log aggregation)
  # ============================================================================
  loki:
    image: grafana/loki:3.0.0
    container_name: crux-loki
    networks:
      monitor-network:
        ipv4_address: 172.23.0.11
      app-network:
    expose:
      - "3100"
    volumes:
      - ./monitoring/loki/loki-config.yaml:/etc/loki/local-config.yaml:ro
    tmpfs:
      - /tmp:size=32M
      - /var/lib/loki:size=64M
    command: "--config.file=/etc/loki/local-config.yaml"
    environment:
      - LOKI_ADMIN_PASSWORD_FILE=/run/secrets/loki_admin_password
      - TZ=UTC
    secrets:
      - loki_admin_password
    restart: unless-stopped
    read_only: true
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3100/ready | grep -q ready || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 20s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
        tag: "{{.Name}}"

  # =================================================================
  # DATA-TIER — Redis (sessions, queues, caching)
  # =================================================================
  redis:
    image: redis:7.2-alpine
    container_name: crux-redis
    networks:
      data-network:
        ipv4_address: 172.22.0.30
    expose:
      - "6379"
    volumes:
      - ./infra/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redis_data:/data
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    secrets:
      - redis_password
    environment:
      - TZ=UTC
    tmpfs:
      - /tmp:size=32M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$(cat /run/secrets/redis_password)\" ping | grep -q PONG || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: "512M"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
        tag: "{{.Name}}"

  # =================================================================
  # APP-TIER — Fastify Backend API
  # =================================================================
  fastify-backend:
    build:
      context: .
      dockerfile: src/server/Dockerfile
      target: runner
    container_name: crux-fastify
    networks:
      app-network:
        ipv4_address: 172.21.0.20
      data-network:
      monitor-network:
    expose:
      - "3000"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=3000
      - LOG_LEVEL=${LOG_LEVEL:-info}

      # PostgreSQL
      - POSTGRES_DB=${POSTGRES_DB:-crux_mail}
      - POSTGRES_USER=${POSTGRES_USER:-crux_user}
      - POSTGRES_PASSWORD_FILE=/run/secrets/postgres_password
      - POSTGRES_HOST=crux-postgres
      - POSTGRES_PORT=5432

      # Redis
      - REDIS_HOST=crux-redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD_FILE=/run/secrets/redis_password

      # Dovecot / IMAP
      - DOVECOT_HOST=crux-dovecot
      - DOVECOT_PORT=993

      # Postfix / SMTP
      - POSTFIX_HOST=crux-postfix
      - POSTFIX_PORT=587

      # Amavis
      - AMAVIS_HOST=crux-amavis
      - AMAVIS_PORT=10024

      # ClamAV
      - CLAMAV_HOST=crux-clamav
      - CLAMAV_PORT=3310

      # MinIO
      - MINIO_HOST=crux-minio
      - MINIO_PORT=9000
      - MINIO_ACCESS_KEY_FILE=/run/secrets/minio_access_key
      - MINIO_SECRET_KEY_FILE=/run/secrets/minio_secret_key

      # OTel
      - OTLP_ENDPOINT=http://otel-collector:4318

      # JWT
      - JWT_SECRET_FILE=/run/secrets/jwt_secret
      - JWT_REFRESH_SECRET_FILE=/run/secrets/jwt_refresh_secret
      - SESSION_ENCRYPTION_KEY_FILE=/run/secrets/session_encryption_key
    secrets:
      - postgres_password
      - redis_password
      - jwt_secret
      - jwt_refresh_secret
      - session_encryption_key
      - minio_access_key
      - minio_secret_key
    tmpfs:
      - /tmp:size=64M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/health | grep -q \"ok\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
        tag: "{{.Name}}"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      dovecot:
        condition: service_healthy

  # =========================================================================
  # APP-TIER — Next.js Frontend
  # =========================================================================
  nextjs-frontend:
    build:
      context: .
      dockerfile: src/web/Dockerfile
      target: runner
    container_name: crux-nextjs
    networks:
      app-network:
        ipv4_address: 172.21.0.21
    expose:
      - "3001"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - NEXT_PUBLIC_API_URL=/api
      - TZ=UTC

    tmpfs:
      - /tmp:size=32M
    read_only: true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3001/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
        tag: "{{.Name}}"
    depends_on:
      fastify-backend:
        condition: service_healthy

  # =================================================================
  # MONITORING — OpenTelemetry Collector (OTLP traces + metrics)
  # =================================================================
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.113.0
    container_name: crux-otel
    networks:
      monitor-network:
        ipv4_address: 172.23.0.40
      app-network:
    expose:
      - "4317"
      - "4318"
    command:
      - '--config=/etc/otel-collector-config.yaml'
    volumes:
      - ./monitoring/otel-collector/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
    environment:
      - OTLP_AUTH_API_KEY_FILE=/run/secrets/otel_api_key
      - TZ=UTC
    secrets:
      - otel_api_key
    tmpfs:
      - /tmp:size=32M
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:13133/ | grep -q \"Service is ready\" || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
        tag: "{{.Name}}"

# ------------------------------------------------------------------
# SECRETS: Gestión segura de credenciales
# ------------------------------------------------------------------
secrets:
  postgres_password:
    file: ./secrets/postgres_password.txt
  minio_access_key:
    file: ./secrets/minio_access_key.txt
  minio_secret_key:
    file: ./secrets/minio_secret_key.txt
  grafana_admin_password:
    file: ./secrets/grafana_admin_password.txt
  redis_password:
    file: ./secrets/redis_password.txt
  jwt_secret:
    file: ./secrets/jwt_secret.txt
  jwt_refresh_secret:
    file: ./secrets/jwt_refresh_secret.txt
  session_encryption_key:
    file: ./secrets/session_encryption_key.txt
  dovecot_master_password:
    file: ./secrets/dovecot_master_password.txt
  otel_api_key:
    file: ./secrets/otel_api_key.txt
  loki_admin_password:
    file: ./secrets/loki_admin_password.txt
  alertmanager_webhook_url:
    file: ./secrets/alertmanager_webhook_url.txt

# ------------------------------------------------------------------
# SECCOMP PROFILE GLOBAL (opcional, requiere Docker >= 20.10)
# ------------------------------------------------------------------
# security_opt en level: seccomp: ./security/seccomp-profile.json