hosts = postgres
user = crux_mail_ro
password = __MAIL_DB_PASSWORD__
dbname = crux_mail
# Mapea una dirección de remitente al login SASL autorizado a usarla
# (identidad: el dueño del buzón = su propio login). Contra `users`.
query = SELECT username FROM users WHERE username='%s' AND is_active=TRUE AND deleted_at IS NULL
